# IronTrack (铁脉健身) · 技术方案与架构设计文档

本文档全面记录了 IronTrack 健身追踪应用的系统架构、技术选型、算法模型、数据持久化策略以及安卓跨端打包实现方案。

---

## 1. 项目概述与设计哲学

### 1.1 核心定位
IronTrack 是一款专为健身爱好者与专业力量训练者打造的**本地优先 (Local-First)**、**纯前端单页架构**的现代化健身追踪与渐进超负荷分析应用。

### 1.2 核心设计哲学 (Gym-First UX)
- **零网络依赖，极端环境可用**：健身房地下室、飞行模式、无 WiFi 场景下均可 100% 离线秒开，数据永不丢失；
- **单手极速录入**：针对健身房手滑、出汗、间歇心率高的特点，告别键盘打字，提供 `+2.5kg`、`+5kg`、`+1次`、`+5分` 步进胶囊按钮；
- **科学指导**：不仅仅是记录流水账，内置 Epley 1RM 极限力量与总容量双维度折线，提供“是否该加重”的决策参考；
- **纯正深色硬核美学**：采用 OLED 纯黑与碳素灰底色，搭配电光绿与科技青霓虹撞色，融入现代磨砂玻璃态拟物视觉。

---

## 2. 系统技术栈选型

| 层次 | 技术选型 | 选用考量与优势 |
| :--- | :--- | :--- |
| **基础框架** | **React 19 + TypeScript** | 强类型安全性、细粒度组件拆分、极高运行性能 |
| **构建工具** | **Vite 8** | 秒级热更新 (HMR)、极小体积编译 (生产包 Gzip 后仅 ~85KB) |
| **样式体系** | **Vanilla CSS + Modern CSS Variables** | 零第三方 CSS 运行时开销，完全自主可控的设计系统与响应式变量 |
| **图标系统** | **Lucide-react** | 轻量化现代几何矢量图标，触控语义直观统一 |
| **图表引擎** | **自研原生响应式 SVG 渲染引擎** | 免去庞大图表库（如 ECharts/Chart.js 占用数百 KB），自研贝塞尔平滑折线、面积渐变与触控数据探针 |
| **音频反馈** | **HTML5 Web Audio API** | 组间休息计时器直接通过浏览器振荡器合成 880Hz 纯音，无需引入任何额外 mp3 资源 |
| **持久化引擎** | **IndexedDB + LocalStorage 双模存储** | 工业级本地结构化大容量异步存储，LocalStorage 镜像容灾 |
| **原生跨端** | **Capacitor 6.x** | 将 Web 产物打包封装为标准 Android 原生工程，一键编译导出 APK |
| **离线 PWA** | **Service Worker + Web App Manifest** | Cache-First 离线缓存机制，支持手机浏览器免安装一键固定到桌面 |

---

## 3. 核心功能模块与架构划分

```
gym-tracker/src/
├── types/
│   └── workout.ts             # 强类型数据模型（动作、组数、历史演化、超负荷判定模型）
├── data/
│   └── presetExercises.ts     # 胸、背、腿、肩、臂、核心、有氧等 40+ 预置黄金动作
├── services/
│   ├── storage.ts             # IndexedDB 异步驱动 + LocalStorage 双写容灾 + JSON 导出导入
│   └── analytics.ts           # 1RM 估算公式、训练总容量与渐进超负荷/耐力决策引擎
├── components/
│   ├── Header.tsx             # 顶部品牌区、桌面模拟器视口开关、数据备份入口、技术文档入口
│   ├── Navbar.tsx             # 底部五维 Tab 导航（记训练、日历、历史、趋势、动作库）
│   ├── WorkoutLogger.tsx      # 力量与有氧自适应录入界面、置顶排序、微调步进胶囊
│   ├── RestTimer.tsx          # 组间休息倒计时器（Web Audio 蜂鸣与振动联动）
│   ├── TrainingCalendar.tsx   # 打卡日历（本月展开统计已练/休息、往月收缩展示、单日回溯复用）
│   ├── WorkoutHistory.tsx     # 训练历史（按日期自动合并、默认折叠、一键复制到今天）
│   ├── AnalyticsView.tsx      # 原生 SVG 力量/有氧趋势折线图与加重/减重建议卡片
│   ├── ExerciseLibrary.tsx    # 动作库浏览、分类检索、动作置顶与自定义动作创建
│   ├── DataBackupModal.tsx    # 数据备份与恢复弹窗（JSON 导入导出、数据清空与重置）
│   └── TechDocsModal.tsx      # 应用内嵌入式技术方案与架构文档阅读器
└── styles/
    ├── variables.css          # 全局设计 Token（色彩、层级、阴影、字体）
    ├── base.css               # 基础重置与轻量过渡动效
    └── app.css                # 深度打磨的移动端交互与卡片样式
```

---

## 4. 科学健身算法模型

### 4.1 1RM 极限力量估算 (Epley 公式)
力量训练中，不同次数（如 5 次与 10 次）无法直接横向对比力量是否增长。系统采用学术界公认最严谨的 **Epley 公式** 换算为单次最大重量 (1 Rep Max)：
$$1RM = Weight \times \left(1 + \frac{Reps}{30}\right)$$
- 若做组次数 $Reps = 1$，则 $1RM = Weight$；
- 换算精度保留一位小数，有效消除次数变化带来的力量波动误差。

### 4.2 力量训练总容量 (Volume)
衡量单次训练对肌纤维的总机械张力负荷：
$$Volume = \sum_{i=1}^{n} \left(Weight_i \times Reps_i\right)$$
自动跳过未打勾完成的无效组，仅统计正式生效负荷。

### 4.3 智能渐进超负荷 (Progressive Overload) 判定模型
系统提取用户针对该动作的历史演化序列，对比本次与前次表现，划分为三大状态并给出可执行决策：
1. **🟢 建议加重 (Increase Weight)**：
   - 触发条件：最大重量提升，或 1RM 提升 $\ge 2kg$ 且总容量维持正向。
   - 决策建议：提示神经与肌肉已适应，给出具体的下次建议冲顶重量（如 `建议下次上调至 72.5kg (微增 2.5kg)`）。
2. **🔴 重量/容量下滑 (Decrease / Recovery)**：
   - 触发条件：最大重量下降，或总容量缩水超 $15\%$。
   - 决策建议：发出中枢疲劳预警，提示可能由于睡眠不足、热量缺口过大或恢复不充分导致，建议下次适度减重 5~10% 或安排减载周 (Deload)。
3. **⚖️ 力量平台期 (Plateau Maintain)**：
   - 触发条件：最大重量与 1RM 连续保持平稳。
   - 决策建议：提示当前重量稳固，建议优先挑战提升每组次数（如 8 次冲击到 10~12 次），或在最后一组加入递减组 (Drop Set) 突破瓶颈。

### 4.4 有氧运动耐力分析模型
对于有氧项目（跑步、单车、划船机等），录入界面智能切换为时长 (min)、距离 (km)、卡路里 (kcal) 与平均心率 (bpm)。
系统通过时长增量与平均配速进行对比，指导用户维持在 **Zone 2 最佳脂肪氧化代谢区间**。

---

## 5. 数据持久化与容灾方案

### 5.1 存储机制
- **主存储：IndexedDB (`IronTrackDB`)**：
  - 采用原生异步事务读写，不阻塞主线程渲染；
  - 设立 `workouts` 与 `custom_exercises` 两个持久对象仓库。
- **镜像存储：LocalStorage**：
  - 在写入 IndexedDB 的同时向 LocalStorage 同步最新快照；
  - 当某些受限环境或私密浏览模式下 IndexedDB 被禁用时，无缝自动降级到 LocalStorage。

### 5.2 数据防丢与跨端迁移
- **JSON 全量导入与导出**：一键导出包含版本号与全量训练日志的 `.json` 结构化文件。
- 用户更换手机、清理浏览器缓存或在电脑与手机之间同步时，直接导入文件即可毫秒级无损恢复。

---

## 6. 安卓打包与离线运行架构

### 6.1 原生 APK 打包流程 (Capacitor 6)
1. 前端经 Vite 优化压缩生成静态产物至 `dist/`；
2. 执行 `npx cap sync android` 将静态产物完整复制到原生安卓工程资产目录：
   `gym-tracker/android/app/src/main/assets/public/`
3. 通过 Android Studio 或命令行 `./gradlew assembleDebug`，Gradle 将 Webview 容器与静态资产整体编译为 `.apk` 二进制包。
4. **运行状态**：完全离线原生运行，不经过任何外部网络请求，启动速度在 200ms 以内。

### 6.2 Service Worker 离线 Web 机制
- 项目内置 `public/sw.js` 与 `manifest.json`；
- 注册 Cache-First 离线缓存拦截器；
- 当部署至公网后，手机浏览器打开一次即会自动将所有静态资产沉淀入手机本地缓存；
- 支持手机端“添加到主屏幕”，秒变无地址栏独立全屏应用，断网飞行模式下同样直接秒开。
